%Load database
trainingData=imageSet('fruits-360\Training','recursive');
for i=1:40 %40 Folders
    for j=1:400 %400 images in each Folder
        image_Container=read(trainingData(i),j);  %Reading each image and storing it
        [thresh_Image, dilated_Image]=pre_processing(image_Container);
        %gray_Image=rgb2gray(image_Container);
        
        
        %Color Features extraction
        
             
        
    end
end

